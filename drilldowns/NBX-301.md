# NBX-301 — NetBox task (reference)

NetBox (router return) tasks are the third task type in the CSP feed. Not redesigned in this session but seeded in `MockTaskRepository` for visual coverage of multi-task-type feed states.

## State set (not documented here)
See the main source repository's `NetBox Framework` docs. This repo focuses on the install redesign only.

## Why it's here
- Visual parity in the redesigned home feed: all three task types render side-by-side
- Anti-pattern check: NetBox uses its own `*_hi_en.json` bundle (`netbox_labels_*.json`) that participates in the same flat-merge collision risk described in [`/data/collision-map.md`](../data/collision-map.md)
- Carry-fee Hindi copy — see [`/ux-copy/hindi-anchors.md`](../ux-copy/hindi-anchors.md) for the converged vocabulary (`पड़े नेटबॉक्स का किराया`)
