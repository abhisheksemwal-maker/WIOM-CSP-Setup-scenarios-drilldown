# INS-1050 — INSTALL_SUBMITTED

## Scenario
CSP has just submitted the install at the end of the physical work. The submission is **queued** for Vyom's verification — it has not started yet. Distinct from INS-1053 (actively verifying).

## Banner (Option C — queued semantics)
| Field | Value |
|---|---|
| Icon | `HourglassBottom` |
| Title | `सेटअप सबमिट हुआ` |
| Subtitle | `वेरिफाई के लिए तैयार` |
| Accent | `stateInfo` |
| Bg | `bgInfo` |

## Deadline pill
`सबमिट हो चुका है` (was `वेरिफिकेशन चल रहा है` — that copy moved to INS-1053).

## Timeline events
`सेटअप सबमिट हुआ` (Devanagari transliteration — was `सेटअप submit हुआ` with Latin "submit").

## CTAs
No primary CTA. State is informational — CSP is waiting on the system.

## The Option C decision
Two states — `INSTALL_SUBMITTED` and `VERIFICATION_PENDING` — previously had the same copy. User direction: **"option C for 1050 and 1053, use hindi spelling of submit"** → keep both states visible but differentiate copy:
- INS-1050: submitted, **queued** for verify (passive wait)
- INS-1053: **actively** verifying (Vyom is checking now)

## Hindi rules applied
- `सबमिट` — Devanagari transliteration (was `submit` in Latin mid-string)
- `वेरिफाई` — short `ि` matra (was `वेरीफाई` — long `ी` creates visible gap in "verify")
- `वेरिफिकेशन` — short `ि` matra throughout

## Source refs
- Banner: `InstallStateBanner.kt:163-169`
- Deadline: `MockTaskRepository.kt` INS-1050 `deadlineDisplay`
- Timeline: `MockTaskRepository.kt` INS-1050 timeline events (lines ~924, 1027)
