# INS-1043 — SLOT_CONFIRMED

## Scenario
Customer has picked one of the two proposed slots. CSP now needs to pick an executor (self or team member) to perform the install.

## Banner
| Field | Value |
|---|---|
| Icon | `Check` |
| Title | `स्लॉट पक्का हुआ` |
| Subtitle | `अब टेक्निशियन चुनना है` |
| Accent | `statePositive` |
| Bg | `bgPositive` |

## Slot section
Filter rule: **only the confirmed slot** is shown. Proposed/cancelled siblings are hidden. This is the filter used across the whole drilldown anytime a slot is CONFIRMED.

## Executor section
Header: **`टेक्निशियन चुनें`** (action header, because `task.executor == null`). Per user direction: "Instead of vyakti chunna hai, technician chunna hain and technician Chunein for the header that comes later in the drill down replacing vyakti."

## CTAs

| Slot | Text | Type | Action |
|---|---|---|---|
| Primary (sticky) | `टेक्निशियन चुनें` | Primary | Opens ExecutorAssignmentSheet |
| Overflow (⋮) | `नहीं कर पाएँगे` | Tertiary | Opens ExitReasonSheet |

## ExecutorAssignmentSheet copy
- Sheet title: `सेटअप कौन करेगा?` (was `व्यक्ति चुनें`)
- Radio list: Annu / others, **no `(स्वयं)` suffix** anywhere (removed via `replace_all`, 7 occurrences)
- Drag handle color: `#D7D3E0` (`border` token)

## Source refs
- Banner: `InstallStateBanner.kt:117-123`
- Executor section: `InstallDrilldownContent.kt` — `showExecutor` gating
- Sheet: `ExecutorAssignmentSheet.kt`
